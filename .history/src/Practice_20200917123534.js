import React from 'react';

function MyApp(){
    const date = new Date()
    const hours = hours.getHours()
    let timeofday;
    return(
        if (hours < 12) {
            timeofday = 'mornin'
        } else {
            
        }
        <h1>What is the time?</h1>
    )
}
export default MyApp;